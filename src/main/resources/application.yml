spring:
  datasource:
    username: root
    url: 'jdbc:mysql://localhost:3306/product_management_system?useSSL=false'
    password: Suspro@19811
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  application:
    name: product-service       
server:
  port: 9090
  
eureka:
    instance:
      hostname: localhost   
